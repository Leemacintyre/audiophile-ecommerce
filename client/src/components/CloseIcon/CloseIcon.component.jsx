import React from 'react';
import { useHistory } from 'react-router-dom'


const CloseIcon = () => {
    const history = useHistory();

    return (
        <div>
            CLOSE ICON STILL TO MAKE
        </div>
    );
};

export default CloseIcon;